# P1 - Hello GitHub Actions

目標：建立第一個 workflow，push 後到 GitHub Actions 頁面看執行結果。

## 操作步驟

1. 建立新的 GitHub Repository。
2. 把這個資料夾內容複製到 repo 根目錄。
3. commit 並 push。
4. 到 GitHub Repository 的 Actions 分頁查看結果。

## 本地 Git 指令

```powershell
git add .
git commit -m "add hello workflow"
git push
```
